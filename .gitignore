# ==========================================
# 1. СЕКРЕТИ ТА КОНФІГУРАЦІЇ (КРИТИЧНО!)
# ==========================================
# Ніколи не комітимо паролі, користувачів та порти для баз даних
.env
*.env
.env.local
.env.*.local

# ==========================================
# 2. PYTHON ТА ВІРТУАЛЬНІ СЕРЕДОВИЩА
# ==========================================
# Ізольований простір для залежностей (Marimo, Pandas, Database Drivers)
venv/
env/
.venv/

# Кеш байт-коду Python
__pycache__/
*.py[cod]
*$py.class

# ==========================================
# 3. MARIMO ТА АРТЕФАКТИ ЗБІРКИ (ДЛЯ LMS)
# ==========================================
# Згенеровані HTML-файли зі вшитим Base64 (для здачі ментору)
hw_submission.html
docs/
# Опціонально: ігноруємо всі локальні HTML експорти, крім тих, що явно додамо
!README.md

# ==========================================
# 4. БАЗИ ДАНИХ ТА DOCKER
# ==========================================
# Локальні дампи баз даних, якщо ви робите бекап перед очищенням
*.sql.gz
*.dump
*.bak

# Якщо в майбутньому ви вирішите мапити Docker volumes на локальні папки:
mysql_data/
pg_data/
db_data/

# ==========================================
# 5. IDE, РЕДАКТОРИ ТА ОПЕРАЦІЙНІ СИСТЕМИ
# ==========================================
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Редактори коду та СУБД (VS Code, PyCharm, DataGrip, DBeaver)
.vscode/
.idea/
.dbeaver/
*.swp
*.swo

# ==========================================
# 6. ЛОКАЛЬНИЙ СТАН ТА БІЛДИ ПРОЄКТУ
# ==========================================
# Локальний реєстр активних баз даних (містить паролі та змінні порти)
databases.json

# Зібраний фронтенд (автономні HTML)
dist/
build/

# ==========================================
# 7. ДАТАСЕТИ ТА ЛОКАЛЬНІ ФАЙЛИ ДАНИХ
# ==========================================
# Ніколи не комітимо сирі дані в репозиторій
*.tsv
*.parquet
*.jsonl

# Локальні файлові бази
*.db
*.sqlite
*.sqlite3

# Папки для експериментів з даними
datasets/

# ==========================================
# 8. ЛОГУВАННЯ
# ==========================================
*.log
logs/

# ==========================================
# 9. ML TOOLING ТА ТЕСТУВАННЯ
# ==========================================
# Чекпоінти ноутбуків
.ipynb_checkpoints/

# Кеші сучасних лінтерів та тестування
.ruff_cache/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
