# ==========================================
# 1. CORE API & SERVER (Стек дашборда)
# ==========================================
fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.3
python-dotenv==1.2.2

# ==========================================
# 2. DATABASE ORM & DRIVERS (Сумісні зі SQLAlchemy)
# ==========================================
sqlalchemy==2.0.49
pymysql==1.1.3
psycopg2-binary==2.9.12
oracledb==3.4.2
pymssql==2.3.13

# ==========================================
# 3. INFRASTRUCTURE & ORCHESTRATION (Docker)
# ==========================================
docker==7.1.0

# --- Pinned Dependencies (FIX for http+docker bug) ---
# Жорстко фіксуємо ці версії, щоб requests не зламав
# протокол спілкування з Unix-сокетом Докера.
requests==2.31.0
urllib3==1.26.18
